#include <stdio.h>
#include <string.h>
  
int main()
{
  
    char str[]="priyanka";
    for(int i=0;str[i]!='\0';i++)
    {
        if(i%2==0){
        int temp=str[i];
        str[i]=str[i+1];
        str[i+1]=temp;
    }
    }
    printf("%s",str);
}

output:

rpyinaak
