#include<stdio.h>
int main()
{
    int n,arm=0,r,n1;
    scanf("%d",&n);
    n1=n;
    while(n)
    {
       r=n%10;
       arm+=r*r*r;
       n=n/10;
    }
    if(arm==n1)
        printf("armstrong");
    else
        printf("not a armstrong");
}
