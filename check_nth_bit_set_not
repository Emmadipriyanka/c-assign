#include <stdio.h>
int main()
{
    int n,bit;
    printf("enter a number:");
    scanf("%d",&n);
    printf("enter a bit:");
    scanf("%d",&bit);
    if(n&(1<<bit))
        printf("bit is set");
    else
        printf("bit is not set");
}

output:

enter a number:10
enter a bit:2
bit is not set
