#include<stdio.h>
int main()
{
    int n,j=0;
    scanf("%d",&n);
    for(int i=2;i<(n/2);i++)
    {
        if(n%i==0)
        {
            j=1;
            break;
        }
    }
    if(j==0)
        printf("prime number");
    else
        printf("not a prime number");
}
