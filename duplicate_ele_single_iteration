#include <stdio.h>
int main()
{
	int arr[] = { 0,1,2,1,2,4,5,6,5 };
	int len= sizeof(arr) / sizeof(arr[0]);

	// count the frequency
	for (int i = 0; i < len; i++) {
		arr[arr[i] % len]
			= arr[arr[i] % len] + len;
	}
	printf("The repeating elements are : \n");
	for (int i = 0; i < len; i++) {
		if (arr[i] >= len* 2) {
			printf("%d \n", i );
		}
	}
	return 0;
}


here for traversing we required hashmap but arr has its own length so arr is enough to traverse

time complexity: O(n)
space complexity : O(1)
