#include<stdio.h>
int main()
{
    int arr[]={1,2,3,4,5};
    int pos;
    scanf("%d",&pos);
    int len=5;
    for(int i=1;i<pos;i++)
    {
        arr[len-i]=arr[len-i-1];
    }
    int ele;
    scanf("%d",&ele);
    arr[pos-1]=ele;
    for(int i=0;i<5;i++){
        printf("%d",arr[i]);
    }
}
