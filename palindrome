#include<stdio.h>
int main()
{
    int n,r=0,sum=0,temp;
    scanf("%d",&n);
    temp=n;
    while(n)
    {
        r=n%10;
        sum=(sum*10)+r;
        n=n/10;
    }
    if(temp==sum)
        printf("palindrome");
    else
        printf("not a palindrome");
}
