#include<stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
    for(i=0;i<32;i++)
    {
        if(n&(1<<i))
            break;
    }
    if(i%2==0)
        printf("power of 4");
    else
        printf("not a power of 4");
}
