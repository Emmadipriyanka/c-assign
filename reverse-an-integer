#include<stdio.h>
int main()
{
    int a=26439864;
    int n;
    scanf("%d",&n);
    int b[n];
    for(int i=0;a!=0;i++)
    {
        b[i]=a%10;
        a=a/10;
        
    }
    for(int i=0;i<n;i++)
        printf("%d",b[i]);
}
