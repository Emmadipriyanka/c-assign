#include<stdio.h>
int main(){
    int arr[]={101, 11, 3, 4, 50, 69, 7, 8, 9, 0},max=0,secmax=0;
    for(int i=0;i<10;i++){
        if(arr[i]>max){
            secmax=max;
            max=arr[i];
        }
        else if(arr[i] > secmax && arr[i] < max)
            secmax=arr[i];
    }
    printf("%d  %d",max,secmax);
}
