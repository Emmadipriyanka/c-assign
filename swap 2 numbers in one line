#include <stdio.h>
int main()
{
    int x = 5, y = 10;
    y=(x+y)-(x=y);
    //y=(x*y)/(x=y);
    //(x ^= y), (y ^= x), (x ^= y);
    printf("After Swapping values of x = %d y = %d", x,y);
    return 0;
}
