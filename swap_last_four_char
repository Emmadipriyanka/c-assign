#include <stdio.h>
#include <string.h>
  
int main()
{
  
    char str[]="nagaiah";
    int len=strlen(str);
    for(int i=0;i<2;i++)
    {
        int temp=str[len-4+i];
        str[len-4+i]=str[len-1-i];
        str[len-1-i]=temp;
    }
    printf("%s",str);
}

output:

naghaia
